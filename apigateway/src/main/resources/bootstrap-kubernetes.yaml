spring:
  config:
    activate:
      on-profile: kubernetes
  cloud:
    config:
      enabled: false
    kubernetes:
      config:
        enabled: true
        name: gateway-config
        namespace: default
      loadbalancer:
        mode: service
      enabled: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false