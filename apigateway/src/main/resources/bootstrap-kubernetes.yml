spring:
  profiles: kubernetes
  cloud:
    kubernetes:
      config:
        enabled: true
        name: gateway
        namespace: default
      loadbalancer:
        mode: service
      enabled: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false