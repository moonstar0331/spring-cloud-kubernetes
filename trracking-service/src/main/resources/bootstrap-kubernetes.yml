spring:
  profiles: kubernetes
  cloud:
    kubernetes:
      config:
        enabled: true
        name: tracking-service
        namespace: default
      loadbalancer:
        mode: service
      enabled: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false